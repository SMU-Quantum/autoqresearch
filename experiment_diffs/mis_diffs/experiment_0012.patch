diff --git a/experiment.py b/experiment.py
index dee20b5..f7869c4 100644
--- a/experiment.py
+++ b/experiment.py
@@ -136,8 +136,8 @@ def build_base_policy(problem, family: str) -> dict:
     if family == "qrao":
         return {
             **common,
-            "qrao_max_vars_per_qubit": 2,
-            "qrac_type": 2,
+            "qrao_max_vars_per_qubit": 3,
+            "qrac_type": 3,
             "rounding": "magic",
             "ansatz_type": "real_amplitudes",
             "vqe_reps": 1,
@@ -158,22 +158,7 @@ def should_continue(
     """
     if isinstance(problem, (int, np.integer)):
         max_attempts = int(problem)
-    if attempt >= max_attempts:
-        return False
-    if not history:
-        return True
-
-    n_vars = getattr(problem, "num_variables", 0) if problem is not None else 0
-    last = history[-1]
-
-    if n_vars <= 16:
-        return False
-    if attempt >= 2:
-        return False
-    if last.optimality_gap <= 0.0:
-        return False
-
-    return bool((not last.raw_feasible) or last.optimality_gap > 0.6)
+    return attempt < max_attempts
 
 
 def adapt_policy(
@@ -192,23 +177,7 @@ def adapt_policy(
         base_policy = problem
         problem = None
 
-    policy = base_policy.copy()
-    if attempt == 0 or not history:
-        return policy
-
-    n_vars = getattr(problem, "num_variables", 0) if problem is not None else 0
-    last = history[-1]
-
-    if (
-        n_vars > 16
-        and policy.get("solver_family") == "qrao"
-        and ((not last.raw_feasible) or last.optimality_gap > 0.6)
-    ):
-        policy["qrao_max_vars_per_qubit"] = 2
-        policy["qrac_type"] = 2
-        policy["rounding"] = "semideterministic"
-
-    return policy
+    return base_policy.copy()
 
 
 # ═══════════════════════════════════════════════════════════════════
